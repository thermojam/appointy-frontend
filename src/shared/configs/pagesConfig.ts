const MASTER_BASE = "/dashboard";

export class PagesConfig {
    static readonly MAIN = `${MASTER_BASE}`;
    static readonly APPOINTMENTS = `${MASTER_BASE}/appointments`;
    static readonly SERVICES = `${MASTER_BASE}/services`;
    static readonly SCHEDULE = `${MASTER_BASE}/schedule`;
    static readonly NOTIFICATIONS = `${MASTER_BASE}/notifications`;
    static readonly GALLERY = `${MASTER_BASE}/gallery`;
    static readonly REVIEWS = `${MASTER_BASE}/reviews`;
    static readonly SETTINGS = `${MASTER_BASE}/settings`;
    static readonly LOGOUT = "/logout";
}
